use crate::aoc::DaySolver;

pub struct Solver;

impl DaySolver for Solver {}

#[cfg(test)]
mod tests {

    fn test_input() -> Vec<String> {
        vec![
            // "line1",
            // "line2",
        ]
        .into_iter()
        .map(String::from)
        .collect()
    }

    #[test]
    fn test_solve_part1() {
        assert!(true)
    }

    #[test]
    fn test_solve_part2() {
        assert!(true)
    }
}
